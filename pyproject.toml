python3 << 'PYTHON'
content = """[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yo"
version = "1.0.0"
description = "Yo - AI Chat Application"
dependencies = [
    "Flask==2.3.0",
    "Flask-CORS==4.0.0",
    "python-dotenv==1.0.0",
    "groq==1.6.0",
    "openai==1.56.0",
    "pypdf==3.17.0",
    "Werkzeug==2.3.0",
    "requests==2.31.0",
]
"""

with open('/Users/subhash/Desktop/yo/pyproject.toml', 'w') as f:
    f.write(content)

print("✓ pyproject.toml created successfully!")
PYTHON